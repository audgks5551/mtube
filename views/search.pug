extend layouts/main 
include mixins/videoBlock

block content 
    .search__header 
        h3 Search for #{searchingBy}
    .search__video
        each item in video
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })